# 로컬 개발용 예시. 실제 값은 .env에 넣고 커밋하지 마세요.
DATABASE_URL="file:./prisma/dev.db"

# 세션 쿠키 서명용 비밀키 — 반드시 배포 전 무작위 값으로 교체
# 생성 예: openssl rand -base64 32
SESSION_SECRET="dev-only-secret-change-before-production"
